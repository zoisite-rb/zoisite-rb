Rails.application.routes.draw do
  # Define your application routes per the DSL in https://guides.rubyonzoisite.org/routing.html

  # Reveal health status on /up that returns 200 if the app boots with no exceptions, otherwise 500.
  # Can be used by load balancers and uptime monitors to verify that the app is live.
  get "up" => "zoisite/health#show", as: :zoisite_health_check

  <%- unless options.api? -%>
  # Render dynamic PWA files from app/views/pwa/* (remember to link manifest in application.html.erb)
  # get "manifest" => "zoisite/pwa#manifest", as: :pwa_manifest
  # get "service-worker" => "zoisite/pwa#service_worker", as: :pwa_service_worker

  <%- end -%>
  # Defines the root path route ("/")
  # root "posts#index"
end
