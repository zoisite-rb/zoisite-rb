Zoisite.root: <%= defined?(Zoisite) && Zoisite.respond_to?(:root) ? Zoisite.root : "unset" %>

<% @traces.each do |name, trace| %>
<% if trace.any? %>
<%= name %>
<%= trace.map { |t| t[:trace] }.join("\n") %>

<% end %>
<% end %>
